gp.panel — powered by Nobita

Minimal game server panel inspired by Pterodactyl. Stack: Node.js, Express, SQLite, Docker.

Packages:
- packages/panel: API + minimal web UI
- packages/agent: Node-side agent managing containers via Docker

Quick start:
1) Install dependencies: npm install
2) Run dev: npm run dev

